class Main {
  public static void main(String[] args) {
    /* Note: This code doesn't make sense overall as a program that accomplishes a goal. Instead it is a series of independent if statements for debugging practice.
    
    For this debugging exercise, each example is placed in its own comments.  Uncomment it to explore that example.  For students, you may prefer to make multiple separate programs with just one if example in each.
     */ 

     /*
     //Example 1: This code should print "This is fun." when a is less than 2.  It should always print "I love CS".

     int a = 1;
     if (a < 2)
     {
       System.out.println("This is fun.");
     }
     else
     {
       System.out.println("I love CS.");
     }
     //End Example 1
     */
     
    
    /*
    //Example 2: This code should increase the value of e if it is not a multiple of 3.  Note: This code works, but it is wierd and we don't want people to write code with an empty true block.  Fix the expression to correct it.

    int e = 7;
    if (e % 3 == 0)
    {

    }
    else
    {
      e++;
    }
    System.out.println("The value of e is " + e);

    //End Example 2
    */
    
    /*
    //Example 3:  This code should set the String variable grade to either A, B, or C based on a 90,80,70 scale.
    int d = 81;
    String grade = "Not yet calcuated"; 
    if (d >= 90)
    {
      grade = "A";
    }
    if (d >= 80)
    {
      grade = "B";
    }
    if (d >= 70) 
    {
      grade = "C";
    }
    System.out.println("Grade is " + grade);
    //End Example 3
    */
    
    /*
    //Example 4: This code works, but is inefficient impedes readability.  Change it to avoid duplicated code.
    
    int f = 11;
    int total = 0;
    if (f > 10)
    {
      f++;
      total = total + f;
      System.out.println("total is now "+ total);
    }
    else
    {
      f--;
      total = total + f;
      System.out.println("total is now "+ total);
    }
    //End Example 4
    */
    

    
  }
}